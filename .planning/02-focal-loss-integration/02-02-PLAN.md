---
phase: 02-focal-loss-integration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tests/test_focal_loss.py
  - README.md
  - results/focal_loss_comparison.csv
autonomous: true
requirements: [IMP-02]

must_haves:
  truths:
    - "Unit tests validate focal loss computation"
    - "README documents focal loss usage with parameters table"
    - "README contains CE vs Focal Loss comparison table with actual metrics"
    - "Training completes without errors when focal loss enabled"
  artifacts:
    - path: "tests/test_focal_loss.py"
      provides: "Unit tests for FocalLoss, LabelSmoothingFocalLoss, FocalLossSeq2SeqTrainer"
      min_lines: 50
    - path: "README.md"
      provides: "Focal loss documentation and comparison table"
      contains: "CE vs Focal Loss"
    - path: "results/focal_loss_comparison.csv"
      provides: "Raw experiment metrics"
  key_links:
    - from: "tests/test_focal_loss.py"
      to: "src/focal_loss.py"
      via: "import classes for testing"
      pattern: "from src.focal_loss import"
    - from: "README.md"
      to: "results/focal_loss_comparison.csv"
      via: "documents results from experiments"
---

<objective>
Test focal loss implementation, document usage, and create comparison table with actual experiment results.

Purpose: Validate that focal loss works correctly through unit tests, provide user documentation, and demonstrate performance impact with CE vs Focal Loss comparison (per CONTEXT.md decision).

Output:
- tests/test_focal_loss.py with unit tests
- README.md with usage documentation and comparison table
- results/focal_loss_comparison.csv with raw metrics
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/02-focal-loss-integration/02-CONTEXT.md
@.planning/02-focal-loss-integration/02-RESEARCH.md
@.planning/02-focal-loss-integration/02-01-SUMMARY.md

<interfaces>
<!-- Depends on Plan 01 outputs: CLI flags in train_t5.py and shell script -->

From src/focal_loss.py:
- class FocalLoss(nn.Module): __init__(gamma=2.0, alpha=None, ignore_index=-100), forward(logits, targets) -> loss
- class LabelSmoothingFocalLoss(nn.Module): __init__(gamma=2.0, smoothing=0.1, ignore_index=-100), forward(logits, targets) -> loss
- class FocalLossSeq2SeqTrainer(Seq2SeqTrainer): __init__(*args, focal_gamma=2.0, label_smoothing=0.0, **kwargs)

From Plan 01 (train_t5.py):
- --use_focal_loss flag enables focal loss
- --focal_gamma (default 2.0) configures focusing parameter
- --label_smoothing (default 0.0) configures smoothing factor

From scripts/run_train_t5.sh (Plan 01):
- --use_focal_loss, --focal_gamma, --label_smoothing flags supported
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for focal loss classes</name>
  <files>tests/test_focal_loss.py</files>
  <read_first>
    - tests/test_model.py (test pattern: class-based with pytest, MagicMock usage, from src.module import pattern)
    - tests/conftest.py (available fixtures: project_root, src_dir)
    - src/focal_loss.py (FocalLoss, LabelSmoothingFocalLoss, FocalLossSeq2SeqTrainer classes)
    - .planning/02-focal-loss-integration/02-RESEARCH.md (Pattern 4: Test with Mocks section)
  </read_first>
  <action>
    Create new file tests/test_focal_loss.py with:
    
    1. Module docstring: """Unit tests for src/focal_loss.py — Focal Loss classes."""
    
    2. Imports: from unittest.mock import MagicMock, patch; import pytest; import torch
    
    3. Class TestFocalLoss:
       - test_returns_scalar_loss: Create FocalLoss(gamma=2.0), pass (2,10,100) logits and (2,10) targets, assert loss.dim() == 0
       - test_ignore_index_masks_correctly: Create FocalLoss(ignore_index=-100), pass logits and targets with -100 values, assert not torch.isnan(loss)
       - test_gamma_zero_equals_ce_behavior: Create FocalLoss(gamma=0.0), compute loss, assert loss > 0 (valid computation)
       - test_higher_gamma_reduces_easy_example_weight: Compare loss with gamma=0 vs gamma=2 for high-confidence prediction
    
    4. Class TestLabelSmoothingFocalLoss:
       - test_smoothing_zero_equals_focal_loss: LabelSmoothingFocalLoss(smoothing=0.0) should behave like FocalLoss
       - test_smoothing_increases_uniform_distribution_weight: With smoothing > 0, verify smooth_loss component affects output
       - test_returns_scalar_loss: Same shape verification as FocalLoss
    
    5. Class TestFocalLossSeq2SeqTrainer:
       - test_init_with_default_params: Verify FocalLossSeq2SeqTrainer instantiates with defaults
       - test_init_with_label_smoothing_uses_smoothing_loss: When label_smoothing > 0, verify LabelSmoothingFocalLoss is used
       - test_compute_loss_returns_loss_and_outputs: Mock model and inputs, verify compute_loss signature
  </action>
  <acceptance_criteria>
    - source: tests/test_focal_loss.py exists
    - source: grep -c "class TestFocalLoss" tests/test_focal_loss.py returns 1
    - source: grep -c "class TestLabelSmoothingFocalLoss" tests/test_focal_loss.py returns 1
    - source: grep -c "class TestFocalLossSeq2SeqTrainer" tests/test_focal_loss.py returns 1
    - source: grep -c "def test_" tests/test_focal_loss.py returns >= 8
    - test-command: pytest tests/test_focal_loss.py -v passes all tests
    - test-command: pytest tests/test_focal_loss.py --collect-only shows >= 8 test items
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_focal_loss.py -v --tb=short</automated>
  </verify>
  <done>New test file created with TestFocalLoss (4 tests), TestLabelSmoothingFocalLoss (3 tests), TestFocalLossSeq2SeqTrainer (3 tests); all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Run training experiments (CE vs Focal Loss)</name>
  <files>results/focal_loss_comparison.csv</files>
  <read_first>
    - src/train_t5.py (verify CLI flags from Plan 01 work)
    - scripts/run_train_t5.sh (shell script usage)
    - data/data.csv (verify training data exists)
    - .planning/02-focal-loss-integration/02-CONTEXT.md (comparison table requirements: F1, accuracy, per-class performance)
  </read_first>
  <action>
    Run two quick training experiments with reduced epochs (2-3 epochs) to compare CE vs Focal Loss:
    
    1. Verify training setup:
       - Confirm data/data.csv exists
       - Confirm models/vit5_finetune_balanced/ exists (base model)
    
    2. Run baseline (standard CrossEntropy):
       python src/train_t5.py \
         --model_name models/vit5_finetune_balanced \
         --data_path data/data.csv \
         --output_dir results/ce_baseline \
         --num_train_epochs 2 \
         --per_device_train_batch_size 8 \
         --per_device_eval_batch_size 8 \
         --gpu 0
       
       Record: F1-macro, accuracy, per-class F1 (CLEAN, OFFENSIVE, HATE)
    
    3. Run focal loss experiment:
       python src/train_t5.py \
         --model_name models/vit5_finetune_balanced \
         --data_path data/data.csv \
         --output_dir results/focal_loss_exp \
         --num_train_epochs 2 \
         --per_device_train_batch_size 8 \
         --per_device_eval_batch_size 8 \
         --use_focal_loss \
         --focal_gamma 2.0 \
         --label_smoothing 0.0 \
         --gpu 0
       
       Record: F1-macro, accuracy, per-class F1 (CLEAN, OFFENSIVE, HATE)
    
    4. Save results to results/focal_loss_comparison.csv:
       loss_type,f1_macro,accuracy,f1_clean,f1_offensive,f1_hate
       CrossEntropy,<value>,<value>,<value>,<value>,<value>
       FocalLoss,<value>,<value>,<value>,<value>,<value>
    
    Note: If full training is too slow, use --max_train_samples 500 --max_val_samples 100 for quick comparison.
  </action>
  <acceptance_criteria>
    - results/focal_loss_comparison.csv exists
    - CSV has header: loss_type,f1_macro,accuracy,f1_clean,f1_offensive,f1_hate
    - CSV has 2 data rows (CrossEntropy and FocalLoss)
    - Training completes without errors for both runs
  </acceptance_criteria>
  <verify>
    <automated>test -f results/focal_loss_comparison.csv && head -3 results/focal_loss_comparison.csv</automated>
  </verify>
  <done>Both training experiments complete; results/focal_loss_comparison.csv contains CE and Focal Loss metrics</done>
</task>

<task type="auto">
  <name>Task 3: Update README with documentation and comparison table</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing structure, find "Usage" or "Training" section to add after)
    - results/focal_loss_comparison.csv (actual metrics from Task 2)
    - .planning/02-focal-loss-integration/02-CONTEXT.md (documentation decision: README.md with comparison table showing F1, accuracy, per-class performance)
    - .planning/02-focal-loss-integration/02-RESEARCH.md (CLI flag design example)
  </read_first>
  <action>
    Add new section "## Training with Focal Loss" after existing training documentation. Include:
    
    1. Section header: ## Training with Focal Loss
    
    2. Brief description (2-3 sentences): Focal loss addresses class imbalance by down-weighting easy examples. This is particularly useful for Vietnamese hate speech datasets where CLEAN >> OFFENSIVE >> HATE.
    
    3. Usage subsection with example command:
       ```bash
       bash scripts/run_train_t5.sh \
         --use_focal_loss \
         --focal_gamma 2.0 \
         --label_smoothing 0.1
       ```
    
    4. Parameters table:
       | Parameter | Default | Description |
       |-----------|---------|-------------|
       | --use_focal_loss | false | Enable focal loss (required to use other params) |
       | --focal_gamma | 2.0 | Focusing parameter (higher = more focus on hard examples) |
       | --label_smoothing | 0.0 | Label smoothing factor (0.0 = no smoothing) |
    
    5. **CE vs Focal Loss Comparison Table** (per CONTEXT.md D-03):
       ### Experimental Results
       
       Comparison on Vietnamese hate speech dataset (2 epochs, same hyperparameters):
       
       | Loss Function | F1 (Macro) | Accuracy | F1 (CLEAN) | F1 (OFFENSIVE) | F1 (HATE) |
       |---------------|------------|----------|------------|----------------|-----------|
       | CrossEntropy  | <from CSV> | <from CSV> | <from CSV> | <from CSV> | <from CSV> |
       | Focal Loss    | <from CSV> | <from CSV> | <from CSV> | <from CSV> | <from CSV> |
       
       Note: Focal Loss typically improves performance on minority classes (HATE, OFFENSIVE) at a small cost to majority class (CLEAN).
    
    6. Reference: Lin et al., "Focal Loss for Dense Object Detection", ICCV 2017
  </action>
  <acceptance_criteria>
    - source: grep "## Training with Focal Loss" README.md returns 1 match
    - source: grep "use_focal_loss" README.md returns >= 2 matches
    - source: grep "focal_gamma" README.md returns >= 2 matches
    - source: grep "label_smoothing" README.md returns >= 2 matches
    - source: grep "Lin et al" README.md returns 1 match (citation)
    - source: grep -E "\| Parameter \|" README.md returns 1 match (parameters table)
    - source: grep -E "\| Loss Function \|" README.md returns 1 match (comparison table - per D-03)
    - source: grep "CrossEntropy" README.md returns >= 1 match (in comparison table)
    - source: grep "Focal Loss" README.md returns >= 2 matches (in comparison table and elsewhere)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Loss Function" README.md returns 1 && grep -c "Training with Focal Loss" README.md returns 1</automated>
  </verify>
  <done>README.md updated with "Training with Focal Loss" section containing usage example, parameters table, CE vs Focal Loss comparison table with actual metrics, and paper reference</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Production code | Tests import and exercise focal_loss module |
| CSV data → README | Experiment results embedded in documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | results/ CSV | accept | Only contains aggregate metrics, no PII |
| T-02-04 | Tampering | test assertions | mitigate | Use deterministic test fixtures, seed random states |
</threat_model>

<verification>
After all tasks complete, verify:
1. `pytest tests/test_focal_loss.py -v` passes all tests (>= 8 tests)
2. `test -f results/focal_loss_comparison.csv` confirms experiment results exist
3. `grep "Training with Focal Loss" README.md` finds documentation section
4. `grep "Loss Function" README.md` finds comparison table
5. Training experiments complete without errors
</verification>

<success_criteria>
- [ ] Unit tests pass (>= 8 tests)
- [ ] Training completes without errors when focal loss enabled
- [ ] results/focal_loss_comparison.csv contains CE vs Focal Loss metrics
- [ ] README documents focal loss usage with parameters table
- [ ] README contains CE vs Focal Loss comparison table with actual F1/accuracy/per-class metrics
</success_criteria>

<output>
Create `.planning/phases/02-focal-loss-integration/02-02-SUMMARY.md` when done
</output>
