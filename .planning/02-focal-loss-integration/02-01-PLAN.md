---
phase: 02-focal-loss-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/train_t5.py
  - scripts/run_train_t5.sh
autonomous: true
requirements: [IMP-02]

must_haves:
  truths:
    - "User can enable focal loss via --use_focal_loss flag"
    - "User can configure gamma via --focal_gamma (default 2.0)"
    - "User can configure label smoothing via --label_smoothing (default 0.0)"
    - "Error message shown when gamma/smoothing passed without --use_focal_loss"
  artifacts:
    - path: "src/train_t5.py"
      provides: "CLI args and conditional trainer selection"
      contains: "use_focal_loss"
    - path: "scripts/run_train_t5.sh"
      provides: "Shell script with focal loss flags"
      contains: "FOCAL_LOSS"
  key_links:
    - from: "src/train_t5.py"
      to: "src/focal_loss.py"
      via: "import FocalLossSeq2SeqTrainer"
      pattern: "from focal_loss import"
    - from: "scripts/run_train_t5.sh"
      to: "src/train_t5.py"
      via: "python src/train_t5.py call"
      pattern: "--use_focal_loss"
---

<objective>
Integrate focal loss into T5 training pipeline with CLI configuration.

Purpose: Enable training with focal loss to address class imbalance in Vietnamese hate speech datasets (CLEAN >> OFFENSIVE >> HATE). Focal loss focuses learning on hard-to-classify examples.

Output:
- train_t5.py with --use_focal_loss, --focal_gamma, --label_smoothing flags
- run_train_t5.sh shell script updated with focal loss support
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/02-focal-loss-integration/02-CONTEXT.md
@.planning/02-focal-loss-integration/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/focal_loss.py:
- class FocalLoss(nn.Module): __init__(gamma=2.0, alpha=None, ignore_index=-100), forward(logits, targets) -> loss
- class LabelSmoothingFocalLoss(nn.Module): __init__(gamma=2.0, smoothing=0.1, ignore_index=-100), forward(logits, targets) -> loss
- class FocalLossSeq2SeqTrainer(Seq2SeqTrainer): __init__(*args, focal_gamma=2.0, label_smoothing=0.0, **kwargs)

From src/train_t5.py (existing argparse pattern lines 34-51):
- parser.add_argument('--flag_name', type=TYPE, default=VALUE, help='Description')
- bash_args = parser.parse_args() at line 53

Trainer creation at lines 383-391:
- trainer = Seq2SeqTrainer(model=model, args=training_args, data_collator=data_collator, train_dataset=train_tokenized_dataset, eval_dataset=val_tokenized_dataset, tokenizer=tokenizer, compute_metrics=compute_metrics)

From scripts/run_train_t5.sh (shell pattern):
- Default values at top, case statement parsing, python call at end with all flags
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI arguments and validation to train_t5.py</name>
  <files>src/train_t5.py</files>
  <read_first>
    - src/train_t5.py (lines 34-55 for argparse pattern, line 53 for bash_args)
    - src/focal_loss.py (lines 121-137 for FocalLossSeq2SeqTrainer signature)
    - .planning/02-focal-loss-integration/02-CONTEXT.md (locked decisions: gamma default 2.0, smoothing default 0.0, error on param conflict)
  </read_first>
  <action>
    1. Add three arguments after line 51 (after --gpu argument):
       - --use_focal_loss: action='store_true', help='Use focal loss instead of standard CrossEntropy'
       - --focal_gamma: type=float, default=2.0, help='Focal loss gamma parameter (focusing strength)'
       - --label_smoothing: type=float, default=0.0, help='Label smoothing factor (0.0 = no smoothing)'
    
    2. Add validation block after line 53 (after bash_args = parser.parse_args()):
       - If not args.use_focal_loss AND (args.focal_gamma != 2.0 OR args.label_smoothing != 0.0): parser.error("--focal_gamma and --label_smoothing require --use_focal_loss flag")
       - If args.focal_gamma < 0: parser.error("--focal_gamma must be >= 0")
       - If args.label_smoothing < 0.0 or args.label_smoothing > 1.0: parser.error("--label_smoothing must be between 0.0 and 1.0")
  </action>
  <acceptance_criteria>
    - source: grep -c "use_focal_loss" src/train_t5.py returns >= 3
    - source: grep -c "focal_gamma" src/train_t5.py returns >= 3
    - source: grep -c "label_smoothing" src/train_t5.py returns >= 3
    - source: grep "action='store_true'" src/train_t5.py shows --use_focal_loss uses store_true
    - source: grep "default=2.0" src/train_t5.py shows focal_gamma default is 2.0
    - source: grep "default=0.0" src/train_t5.py shows label_smoothing default is 0.0
    - CLI: python src/train_t5.py --help shows all three focal loss arguments
    - CLI: python src/train_t5.py --focal_gamma 3.0 (without --use_focal_loss) exits with error containing "require --use_focal_loss"
  </acceptance_criteria>
  <verify>
    <automated>python src/train_t5.py --help | grep -E "(use_focal_loss|focal_gamma|label_smoothing)" | wc -l returns 3</automated>
  </verify>
  <done>Three focal loss CLI arguments added with correct defaults; validation rejects gamma/smoothing without --use_focal_loss flag; validation rejects invalid ranges</done>
</task>

<task type="auto">
  <name>Task 2: Implement conditional trainer selection</name>
  <files>src/train_t5.py</files>
  <read_first>
    - src/train_t5.py (lines 370-395 for trainer creation context)
    - src/focal_loss.py (lines 121-158 for FocalLossSeq2SeqTrainer implementation)
    - .planning/02-focal-loss-integration/02-RESEARCH.md (Pattern 3: Conditional Trainer Selection)
  </read_first>
  <action>
    1. Add import at top of file (after existing imports around line 20): from focal_loss import FocalLossSeq2SeqTrainer
    
    2. Replace trainer creation block (lines 383-391) with conditional logic:
       - If bash_args.use_focal_loss:
         - print(f'PROGRESS|Using Focal Loss (gamma={bash_args.focal_gamma}, smoothing={bash_args.label_smoothing})')
         - trainer = FocalLossSeq2SeqTrainer(model=model, args=training_args, data_collator=data_collator, train_dataset=train_tokenized_dataset, eval_dataset=val_tokenized_dataset, tokenizer=tokenizer, compute_metrics=compute_metrics, focal_gamma=bash_args.focal_gamma, label_smoothing=bash_args.label_smoothing)
       - Else:
         - print('PROGRESS|Using standard CrossEntropy loss')
         - trainer = Seq2SeqTrainer(...) (existing code)
  </action>
  <acceptance_criteria>
    - source: grep "from focal_loss import FocalLossSeq2SeqTrainer" src/train_t5.py returns 1 match
    - source: grep "if bash_args.use_focal_loss" src/train_t5.py returns 1 match
    - source: grep "focal_gamma=bash_args.focal_gamma" src/train_t5.py returns 1 match
    - source: grep "PROGRESS|Using Focal Loss" src/train_t5.py returns 1 match
    - source: grep "PROGRESS|Using standard CrossEntropy" src/train_t5.py returns 1 match
    - behavior: Running with --use_focal_loss prints "PROGRESS|Using Focal Loss (gamma=2.0, smoothing=0.0)"
    - behavior: Running without --use_focal_loss prints "PROGRESS|Using standard CrossEntropy loss"
  </acceptance_criteria>
  <verify>
    <automated>grep -c "FocalLossSeq2SeqTrainer" src/train_t5.py returns >= 2</automated>
  </verify>
  <done>Conditional trainer selection implemented; FocalLossSeq2SeqTrainer used when --use_focal_loss flag present; progress messages indicate which loss function is active</done>
</task>

<task type="auto">
  <name>Task 3: Update shell script with focal loss flags</name>
  <files>scripts/run_train_t5.sh</files>
  <read_first>
    - scripts/run_train_t5.sh (full file for existing patterns: defaults at top, case statement, python call)
    - .planning/02-focal-loss-integration/02-RESEARCH.md (Shell Script Pattern section)
  </read_first>
  <action>
    1. Add default values after line 17 (after GPU="0"):
       - USE_FOCAL_LOSS=false
       - FOCAL_GAMMA=2.0
       - LABEL_SMOOTHING=0.0
    
    2. Add case statement entries after --gpu case (before *) around line 72):
       - --use_focal_loss): USE_FOCAL_LOSS=true; shift 1;;
       - --focal_gamma): FOCAL_GAMMA="$2"; shift 2;;
       - --label_smoothing): LABEL_SMOOTHING="$2"; shift 2;;
    
    3. Add echo statements in configuration output section (before "===========" closing):
       - echo "Use focal loss: $USE_FOCAL_LOSS"
       - echo "Focal gamma: $FOCAL_GAMMA"
       - echo "Label smoothing: $LABEL_SMOOTHING"
    
    4. Build focal loss arguments conditionally before python call:
       - FOCAL_ARGS=""
       - if [ "$USE_FOCAL_LOSS" = true ]; then
           FOCAL_ARGS="--use_focal_loss --focal_gamma $FOCAL_GAMMA --label_smoothing $LABEL_SMOOTHING"
         fi
    
    5. Add $FOCAL_ARGS to the python command (append after --gpu "$GPU")
  </action>
  <acceptance_criteria>
    - source: grep "USE_FOCAL_LOSS=false" scripts/run_train_t5.sh returns 1 match
    - source: grep "FOCAL_GAMMA=2.0" scripts/run_train_t5.sh returns 1 match
    - source: grep "LABEL_SMOOTHING=0.0" scripts/run_train_t5.sh returns 1 match
    - source: grep -E "\-\-use_focal_loss\)" scripts/run_train_t5.sh returns 1 match (case entry)
    - source: grep -E "\-\-focal_gamma\)" scripts/run_train_t5.sh returns 1 match (case entry)
    - source: grep '$FOCAL_ARGS' scripts/run_train_t5.sh returns 1 match
    - CLI: bash scripts/run_train_t5.sh --help or running without args shows "Use focal loss: false" in output
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/run_train_t5.sh && echo "Syntax OK"</automated>
  </verify>
  <done>Shell script updated with USE_FOCAL_LOSS, FOCAL_GAMMA, LABEL_SMOOTHING defaults; case parsing for all three flags; conditional FOCAL_ARGS passed to python</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → train_t5.py | User-provided arguments cross into training script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CLI args | mitigate | Validate focal_gamma >= 0 and label_smoothing in [0,1] before trainer creation |
| T-02-02 | Denial of Service | focal_gamma | accept | Very large gamma values slow training but don't crash; documented as user discretion |
</threat_model>

<verification>
After all tasks complete, verify:
1. `python src/train_t5.py --help` shows --use_focal_loss, --focal_gamma, --label_smoothing
2. `python src/train_t5.py --focal_gamma 3.0` (without --use_focal_loss) exits with error
3. `bash -n scripts/run_train_t5.sh` passes syntax check
4. `grep "FocalLossSeq2SeqTrainer" src/train_t5.py` confirms import and usage
</verification>

<success_criteria>
- [ ] `--use_focal_loss` flag works in train_t5.py
- [ ] `--focal_gamma` configurable with default 2.0
- [ ] `--label_smoothing` optional with default 0.0
- [ ] Validation rejects gamma/smoothing without use_focal_loss
- [ ] Shell script passes flags correctly
</success_criteria>

<output>
Create `.planning/phases/02-focal-loss-integration/02-01-SUMMARY.md` when done
</output>
