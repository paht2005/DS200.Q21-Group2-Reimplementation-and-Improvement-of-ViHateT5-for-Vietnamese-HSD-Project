---
phase: 01-fix-tests-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: true
requirements:
  - IMP-01

must_haves:
  truths:
    - "All test_inference.py tests pass"
    - "NumPy version is pinned to <2 to prevent future breakage"
    - "Test suite runs without errors (128/129 pass, 1 skipped)"
  artifacts:
    - path: "requirements.txt"
      provides: "NumPy version constraint"
      contains: "numpy>=1.21.0,<2"
  key_links:
    - from: "requirements.txt"
      to: "tests/test_inference.py"
      via: "numpy constraint prevents PyTorch tensor conversion errors"
      pattern: "numpy.*<2"
---

<objective>
Fix 4 failing tests in test_inference.py caused by NumPy 2.x / PyTorch 2.2.2 incompatibility.

Purpose: Restore 100% test pass rate as foundation for Phase 2+ feature work.
Output: Updated requirements.txt with numpy<2 constraint, all tests passing.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/01-fix-tests-code-quality/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose Test Failures</name>
  <files>tests/test_inference.py, src/inference.py</files>
  <read_first>
    - tests/test_inference.py (lines 1-80)
    - src/inference.py (predict_single function)
  </read_first>
  <action>
Run failing tests to capture error signature:
```bash
python -m pytest tests/test_inference.py -v 2>&1 | head -100
```

Identify the error type (AttributeError on numpy, RuntimeError on tensor conversion).
Check installed numpy version: `pip show numpy | grep Version`.
Cross-reference with PyTorch compatibility: PyTorch 2.2.2 requires NumPy < 2.0.

Root cause: NumPy 2.0+ removed/changed APIs that PyTorch tensor.numpy() relies on.
  </action>
  <acceptance_criteria>
    - behavior: Running `python -m pytest tests/test_inference.py::TestPredictSingle -v` shows 4 failing tests with numpy-related errors
    - cli: `pip show numpy | grep Version` shows version 2.x if broken, or 1.x after fix
    - source: Error traceback contains "numpy" and relates to tensor conversion in predict_single()
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_inference.py -v --tb=short 2>&1 | grep -E "(FAILED|PASSED|ERROR)"</automated>
  </verify>
  <done>Root cause identified as NumPy 2.x breaking change with PyTorch 2.2.2</done>
</task>

<task type="auto">
  <name>Task 2: Pin NumPy Version in requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (full file)
  </read_first>
  <action>
Edit requirements.txt to add upper bound on numpy:

Change:
```
numpy>=1.21.0
```

To:
```
numpy>=1.21.0,<2  # NumPy 2.x incompatible with PyTorch <2.4
```

Then reinstall:
```bash
pip install "numpy>=1.21.0,<2"
```
  </action>
  <acceptance_criteria>
    - source: requirements.txt contains line `numpy>=1.21.0,<2`
    - cli: `pip freeze | grep numpy` shows version 1.26.x (not 2.x)
    - cli: `grep "numpy.*<2" requirements.txt` returns the constraint line
  </acceptance_criteria>
  <verify>
    <automated>grep -c "numpy>=1.21.0,<2" requirements.txt && pip freeze | grep -E "^numpy==" | grep -v "^numpy==2"</automated>
  </verify>
  <done>requirements.txt updated with numpy<2 constraint, numpy 1.26.4 installed</done>
</task>

<task type="auto">
  <name>Task 3: Verify All Tests Pass</name>
  <files>tests/</files>
  <read_first>
    - tests/test_inference.py (to confirm test names)
  </read_first>
  <action>
Run full test suite to confirm fix:
```bash
python -m pytest tests/ -v
```

Expected outcome:
- 128 tests pass
- 1 test skipped (test_word_seg_requires_underthesea - expected when underthesea is installed)
- 0 tests fail

The skipped test is expected behavior: it tests error handling when underthesea is NOT installed, but since underthesea IS installed, the test correctly skips.
  </action>
  <acceptance_criteria>
    - test-command: `python -m pytest tests/ -v` exits with code 0
    - cli: pytest output shows "128 passed, 1 skipped" (or similar)
    - behavior: All 4 previously failing tests in test_inference.py now pass
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>Test suite passes with 128/129 tests (1 correctly skipped for optional dependency)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → Local env | Third-party packages installed from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | requirements.txt | mitigate | Pin specific version ranges, not arbitrary latest |
| T-01-02 | Denial of Service | pip install | accept | Local dev environment, no production impact |
</threat_model>

<verification>
1. `python -m pytest tests/test_inference.py -v` — all 4 tests pass
2. `pip freeze | grep numpy` — shows 1.26.x
3. `python -m pytest tests/ -v` — 128 passed, 1 skipped
</verification>

<success_criteria>
- [ ] requirements.txt contains `numpy>=1.21.0,<2`
- [ ] NumPy version is 1.26.x (not 2.x)
- [ ] test_inference.py tests pass (4/4)
- [ ] Full test suite: 128 passed, 1 skipped, 0 failed
</success_criteria>

<output>
Create `.planning/phases/01-fix-tests-code-quality/01-01-SUMMARY.md` when done
</output>
